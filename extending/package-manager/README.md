# Extending Posit Package Manager images

## Examples

### Server examples

These examples build on the `package-manager` Minimal image (`-min`).

| Path | Example |
|:-----|:--------|
| [server/ca-certificates](./server/ca-certificates/) | Add a custom CA certificate to the system trust store |
